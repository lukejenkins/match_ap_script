# AP Replacement Matching Script Configuration
# Copy this file to .env and customize for your environment

# Input Files
# ------------
# Option 1: Use a combined shows file containing all command outputs
COMBINED_FILE=./examples/private/shows.txt

# Option 2: Use separate files (comment out COMBINED_FILE if using these)
# CDP_FILE=./examples/private/show_ap_cdp_neighbors.txt
# MERAKI_FILE=./examples/private/show_ap_meraki_monitoring_summary.txt

# Tracking Spreadsheet (REQUIRED)
INPUT_CSV=./examples/private/tracking.csv

# Output Files
# ------------
# Output CSV file (optional - defaults to <INPUT_CSV>_updated.csv)
OUTPUT_CSV=./output/updated.csv

# Log directory for debug files (optional)
LOG_DIR=./logs/

# Notes:
# - CLI arguments override .env settings
# - You must have either COMBINED_FILE or at least one of CDP_FILE/MERAKI_FILE
# - Paths can be relative or absolute
# - Make sure to add your actual .env file to .gitignore to keep credentials safe
